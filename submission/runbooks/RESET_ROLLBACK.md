# RESET / ROLLBACK

```text
python submission/scripts/reset_submission.py
```

Clears generated evidence/runs only. Challenge data outside `submission/` is immutable — never modify it to “fix” failures.
