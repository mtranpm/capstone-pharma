# RETIREMENT AND VENDOR EXIT

See artefact `36`. Export evidence hashes before teardown. Retain audit trails per GxP retention. Substitute LLM/Neo4j adapters via ports without rewriting SoR fixtures.
