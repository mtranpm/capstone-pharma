# DEPLOYMENT

- Preferred workshop path: local Python + CLI (no Docker required).
- Optional: `docker compose -f submission/docker-compose.yml up api`
- Neo4j profile optional: `--profile neo4j`
- Do not place Neo4j credentials in the browser or git.
