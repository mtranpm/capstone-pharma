# Brownfield Starter

The starter is intentionally unsafe, incomplete and inconsistent. It is evidence, not a solution.

Run `python baseline_diagnostics.py`. The diagnostic exposes only obvious defects. Participants must perform a broader architecture, GxP, security, privacy, data-integrity, reliability and economics assessment.
